{
  "name": "nodenet-core",
  "version": "1.0.0",
  "description": "",
  "main": "test/controller.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "type-check:watch": "npm run type-check -- --watch",
    "build": "npm run build:types && npm run build:js",
    "build:types": "tsc",
    "build:js": "babel src --out-dir lib --extensions \".js\" --source-maps inline"
  },
  "keywords": [
    "NodeNet",
    "NodeNetCore",
    "Core"
  ],
  "author": "Mario Bertsch",
  "license": "MIT",
  "dependencies": {
    "@babel/plugin-proposal-decorators": "^7.21.0",
    "@babel/plugin-transform-typescript": "^7.21.0",
    "@babel/preset-env": "^7.20.2",
    "babel-cli": "^6.26.0"
  },
  "devDependencies": {
    "prettier": "^2.8.4"
  }
}
